[tool.cibuildwheel]
build = ["cp312-*"]
manylinux-x86_64-image = "manylinux2014"
skip = ["*musllinux*"]

[tool.cibuildwheel.linux]
#environment = { REINDEXER_INCLUDE_DIR = "/usr/local/include" }
archs = ["x86_64"]

#[tool.cibuildwheel.macos]
#archs = ["x86_64"]

[build-system]
requires = [
    "setuptools",
    "wheel",
    "pybind11"
]

build-backend = "setuptools.build_meta"